PyQt6>=6.6
av>=13.0        # PyAV - bundles FFmpeg's libraries statically; no system
                # ffmpeg/ffprobe install or PATH lookup needed.
Pillow>=10.0    # thumbnail JPEG encode (PyAV's VideoFrame.to_image() needs it)
